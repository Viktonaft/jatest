apply plugin: "java"

repositories {
    mavenCentral()
}

mainClassName = "com.builders.services"

dependencies {
	compile project (":utils")
	compile "javax.servlet:javax.servlet-api:3.0.1"
	testCompile "junit:junit:4.12"
}

