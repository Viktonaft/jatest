allprojects {
	apply plugin: "java"
	apply plugin: "application"
	apply plugin: "idea"
}

mainClassName = "com.builders"

buildscript {
	repositories {
		mavenCentral()
	}
}

task wrapper(type: Wrapper) {
	gradleVersion = "1.12"
}